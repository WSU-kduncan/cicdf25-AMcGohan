[Unit]
Description=Webhook Listener
After=network.target

[Service]
User=root
Group=root
ExecStart=webhook -hooks /home/ec2-user/hooks.yaml -verbose
Restart=on-failure

[Install]
WantedBy=multi-user.target
